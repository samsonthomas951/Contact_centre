// Phase-4 scaffold: a placeholder so the auth-guarded route is reachable.
// The real inbox + ticket detail land in the next commit.

export default function InboxPage() {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-semibold">Inbox</h1>
      <p className="mt-2 text-sm text-slate-600">
        The inbox lands in the next commit. This page exists so the
        auth-guarded shell is end-to-end navigable.
      </p>
    </div>
  );
}
