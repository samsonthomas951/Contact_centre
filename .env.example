# Copy to .env and fill in.

# Edge / TLS
ACME_EMAIL=ops@example.co.ke
ZITADEL_DOMAIN=auth.example.co.ke

# Local-dev override for non-TLS testing (uncomment in docker-compose.override.yml instead)
# ZITADEL_EXTERNALSECURE=false
